# Simplified clippy configuration
# Removed invalid [formatting] and [lints] sections

# Deny some lints that we don't want
# (These can be added to Cargo.toml [lints] in newer Rust versions)
# but for now we'll just keep the file minimal or empty if we move them to Cargo.toml
