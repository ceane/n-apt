# Perf-only Cargo config for the release benchmark job.
# Intentionally left empty so the benchmark job uses the runner's default CPU
# target instead of emitting instructions that may not be available everywhere.
